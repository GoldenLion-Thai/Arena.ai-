# GRiD-OS fleet inventory — SINGLE SOURCE OF TRUTH for host facts.
#
# THIS FILE MUST NEVER CONTAIN SECRETS.
#   No private keys. No passwords. No API tokens. No recovery codes.
#   Key *paths* are fine. Key *contents* are not.
# Secrets live in Vaultwarden only; see docs/vault-credentials.md.
#
# Fields (pipe separated, no spaces around the pipes):
#   id|label|provider|plan|public_ip|ssh_user|key_path|role|status|notes
#
# status: live | unknown | probing | decommissioned
# Use UNKNOWN rather than guessing. A wrong IP is worse than a blank one.

kami-vps-1|kami-VPS-1|Oracle Cloud|VM.Standard.A1.Flex 4 OCPU/24GB/200GB|132.145.57.78|ubuntu|~/.ssh/kami_vps|platform|live|Coolify, Vaultwarden, Portainer, Postgres, Redis. Rebuilt 2026-09-15. Ubuntu 24.04.5 aarch64. PENDING REBOOT.
asci-vps-1|ASCi-VPS-1|Hostinger|KVM 4 4vCPU/16GB/200GB NVMe|72.61.203.79|root|~/.ssh/asci_vps_1|edge|live|Caddy, Docker, Node 20, Tailscale, ESM. Ubuntu 24.04.5 x86_64. Disk 44.9% used. 2 security updates pending.
asci-vps-2|asci-vps-2|Hostinger (SEPARATE account)|unknown|UNKNOWN|root|~/.ssh/asci_vps_2|unknown|unknown|Not visible from the OCI Cloud Shell. Needs its own Hostinger login to discover the IP.
asci-vps-3|asci-vps-3|unknown|unknown|UNKNOWN|UNKNOWN|UNKNOWN|unknown|unknown|Not yet discovered. Confirm it exists before adding it to anything.
